/**
 * 这个方法类似 .max 除了它接受 iteratee 来调用 array中的每一个元素，
 * 来生成其值排序的标准。 iteratee 会调用1个参数: (value)
 * @param {Array} array 
 * @param {Function} [iteratee=_.identity] 
 * @returns {*}
 * @example
 * var objects = [{ 'n': 1 }, { 'n': 2 }]
 * maxBy(objects, function(o) { return o.n; })
 * // => { 'n': 2 }
 * 
 * maxBy(objects, 'n')
 * // => { 'n': 2 }
 */
function maxBy(array, iteratee) {

}
