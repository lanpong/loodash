/**
 * 两个数相除
 * @param {Number} dividend 
 * @param {Numver} divisor 
 * @returns {Number}
 * @example
 * divide(6, 4)
 * // => 1.5
 */
function divide(dividend, divisor) {
    return dividend / divisor;
}
