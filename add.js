/**
 * 两个数相加
 * @param {number} augend 相加的第一个数
 * @param {number} addend 第二个数
 * @returns {number} 返回总和
 * @example
 * add(6, 4);
 * // => 10
 */
function add(augend, addend) {
    return augend + addend;
}
